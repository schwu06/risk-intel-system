# 变更日志

## [Unreleased] - 2026-08-05

### 新增

- 历史报告列表新增重命名入口，支持逐版本修改管理名称。
- 深度研报采用“创建草稿 → 添加专属数据源 → 生成报告”的工作流。
- 支持从已完成报告创建新版；旧数据源会复制成新记录和独立文件，新旧报告互不影响。
- `industry_reports` 增加父报告、版本、生成配置和数据源使用清单。
- `industry_data_sources` 通过必填 `report_id` 外键绑定唯一报告，并记录内容哈希、字符数及复制来源。
- 报告生成时记录每条数据源实际进入模型的字符数和截断状态。
- 新上传研报信源增加来源登记元数据，分别保存原始内容与解析文本哈希。
- 新增 `industry_source_chunks`，保存 PDF 页码、PPTX 幻灯片、XLSX 工作表/行、DOCX 段落/表格行及文本字符位置。
- 勾选补充网络搜索后，搜索结果会自动固化到当前报告的右侧数据源组，并参与本次生成。
- 网络搜索结果自动生成最长 72 字的侧栏摘要标题，并标记来源为“补充网络搜索功能”。
- 未配置 `MITA_API_KEY` 时，页面会禁用补充网络搜索并给出明确提示。
- 文件数据源新增 PPTX、原生 PDF 和扫描 PDF OCR 支持。
- 网址数据源支持直接读取以 `.pdf` 结尾的链接，以及通过响应类型或文件头识别的 PDF。
- 文件上传限制为 25 MB；扫描 PDF 最多 OCR 30 页。

### 变更

- `industry_reports` 新增可空的 `report_name` 字段；自定义名称不会改变报告的行业、企业、正文、版本链或数据源。
- 自定义名称同步用于历史列表、当前报告页标识和 Word 导出文件名；创建新版时默认继承上一版名称。
- 深度研报右侧数据源栏改为按报告 ID 加载，不再按行业名称共用材料。
- 已完成报告及其数据源保持只读；如需补充材料，必须创建新版。
- 多数据源进入模型时按来源分配上下文额度，避免单个大文件完全挤占其他材料。
- 旧研报信源不在启动时自动重解析；迁移只增量补列、建表和建索引，不删除历史报告或来源。
- `parse_uploaded_file()` 继续返回字符串，同时新增结构化解析接口供来源登记和后续证据抽取使用。
- 补充网络搜索从临时提示文本改为可追溯的数据源快照；派生新版会保留旧结果，并按 URL/内容哈希只追加新结果。
- 只要用户勾选补充网络搜索，生成时就会执行搜索；搜索失败会记录在生成配置中，但不会阻断报告生成。
- 研报文件存储目录改为 `data/uploads/industry_reports/{report_id}/`。
- 数据源侧栏只在深度研报页面显示。
- “导出 Word”和“补充数据并生成新版”在报告工具栏中并排显示；新版按钮改为绿色次级样式，并增加与正文的间距。

### 修复

- 深度研报可调用秘塔普通网页搜索 `/api/v1/search`，并把结果自动保存到当前报告的数据源组。
- 扩展秘塔响应解析，兼容 `webpages`、`results`、`items` 及嵌套结果结构。
- 修复深度研报数据源侧栏无法正确渲染的问题。
- 修复 PDF 网址被当成普通 HTML 解析的问题。
- 为网址请求增加浏览器兼容请求头，降低部分站点直接返回 403 的概率。
- 上传文件解析失败时会删除残留文件。
- 增加二次状态检查，防止 PDF/OCR 解析期间报告已开始生成后，迟到的数据源仍被写入。

### API 调整

- 新增 `PATCH /api/v1/industry/reports/{id}/name`，用于修改单个历史报告版本的管理名称。
- 新增 `POST /api/v1/industry/reports/drafts`。
- 新增 `POST /api/v1/industry/reports/{id}/generate`。
- 新增 `POST /api/v1/industry/reports/{id}/fork`。
- 研报数据源接口统一迁移到 `/api/v1/industry/reports/{id}/data-sources...`。
- 保留 `POST /api/v1/industry/analyze` 作为兼容入口；它会创建一份无共享数据源的草稿并立即生成。

### 破坏性升级

- 旧版 `industry_data_sources` 仅按 `industry_name` 归类，无法可靠恢复每份历史报告的输入快照。
- 初始化检测到旧表结构时，会清空并重建 `industry_reports`、`industry_data_sources`，同时清空旧兼容表 `industry_analysis_reports`。
- 本地现有深度研报数据和旧上传目录已经清理；风险日报、主体评估及其数据未受影响。
- 旧的按行业名称管理研报数据源的 API 不再由页面使用。

### 依赖

- 新增 `python-pptx==1.0.2`，用于读取 PPTX。
- 新增 `PyMuPDF==1.28.0`，用于 PDF 渲染与原生文本提取。
- 新增 `rapidocr==3.9.2` 与 `onnxruntime==1.28.0`，用于扫描 PDF OCR。

### 验证

- 已验证同一行业多份报告的数据源互相隔离。
- 已验证父报告与新版的数据源 ID、文件路径和生成结果独立。
- 已使用真实 DeepSeek 完成 v1、v2 两次生成，父报告在新版生成后保持不变。
- 已验证文件上传、网址读取、报告冻结、Word 导出、页面只读状态和并发写入保护。
- 已验证 METI PDF 链接可读取，未返回 403，并能提取日文正文及执行 OCR。

### 已知问题

- 当前环境的 Matplotlib 默认字体不包含完整中文字形。Word 导出可以完成，但生成图表的中文标签可能显示为方框；网页正文和 DeepSeek 报告不受影响。
